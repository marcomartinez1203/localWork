{
  "name": "localwork",
  "version": "1.0.0",
  "description": "LocalWork Application",
  "scripts": {
    "build:backend": "cd backend && npm install && npm run build",
    "build": "npm run build:backend"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
